# Web layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0
python-multipart>=0.0.20

# Google Cloud Agent Builder (ADK + Vertex AI Agent Engine)
google-adk>=2.0.0
google-cloud-aiplatform[agent-engines]>=1.71.0
google-genai>=1.0.0

# MongoDB MCP client (talks to the in-container mongodb-mcp-server)
mcp>=1.27.0

# MongoDB (seed script + pymongo fallback for tool layer)
pymongo>=4.8.0
certifi>=2024.0.0

# Alpaca paper trading (reused from EarningsEdge)
alpaca-py>=0.30.0

# YouTube Live audio extraction
yt-dlp>=2024.10.0

# Audio framing utilities (reused from EarningsEdge audio.py)
numpy>=1.26.0

# Telegram push (optional)
python-telegram-bot>=21.0
